apiVersion: v1
kind: Service
metadata:
  name: argo-java-app-service
  labels:
    app: argo-java-app
spec:
  type: ClusterIP
  selector:
    app: argo-java-app
  ports:
    - name: http
      port: 80
      targetPort: 8080
